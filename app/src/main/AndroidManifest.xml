<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Auto-enable: write adb_wifi_enabled. Granted at runtime via `pm grant`
         (the permission carries the "development" protection flag), never at
         install — so the lint warning is expected and suppressed. -->
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Floating tap button over other apps -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorkManager"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.WorkManager">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".AdbConnectReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.workmanager.app.ACTION_DISCONNECT" />
            </intent-filter>
        </receiver>

        <!-- Auto-enable on boot -->
        <service
            android:name=".AdbAutoEnableService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Keeps the process alive while driving taps in another app -->
        <service
            android:name=".AutomationService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Floating tap button -->
        <service
            android:name=".FloatingButtonService"
            android:exported="false" />

        <!-- Tap-to-read coordinate picker overlay -->
        <service
            android:name=".CoordinatePickerService"
            android:exported="false" />

        <!-- Runs automation scripts as a foreground service -->
        <service
            android:name=".ScriptRunnerService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Auto-pair: reads the Wireless Debugging pairing dialog and pairs -->
        <service
            android:name=".PairAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_config" />
        </service>

        <!-- Profile owner (DPC) for proper work-profile provisioning -->
        <receiver
            android:name=".WorkAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.PROFILE_PROVISIONING_COMPLETE" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".ProfileBootstrapActivity"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay" />

        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
